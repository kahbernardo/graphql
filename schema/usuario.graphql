type Usuario {
    id: Int
    nome: String
    salario: Float
    email: String
    idade: Int
    vip: Boolean
    perfil: Perfil
    status:UsuarioStatus
  }
    type Perfil {
    id: Int
    nome: String
  }

  enum UsuarioStatus{
    ATIVO
    INATIVO
    BLOQUEADO
  }